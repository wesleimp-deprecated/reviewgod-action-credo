defmodule ReviewdogCredo.MixProject do
  use Mix.Project

  def project do
    [
      app: :reviewdog_credo,
      version: "1.0.0",
      elixir: "~> 1.10",
      deps: deps()
    ]
  end

  # Run "mix help deps" to learn about dependencies.
  defp deps do
    [ ]
  end
end
